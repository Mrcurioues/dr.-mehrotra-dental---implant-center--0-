{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "devCommand": "npm run dev",
  "env": {
    "GEMINI_API_KEY": {
      "description": "Google Gemini API Key for AI features",
      "required": true
    },
    "APP_URL": {
      "description": "Production URL of the application",
      "required": true
    },
    "GOOGLE_SHEET_ID": {
      "description": "Google Sheet ID for appointment tracking",
      "required": true
    },
    "GOOGLE_CLIENT_EMAIL": {
      "description": "Service account email from Google Cloud",
      "required": true
    },
    "GOOGLE_PRIVATE_KEY": {
      "description": "Private key from Google Cloud service account",
      "required": true
    },
    "RESEND_API_KEY": {
      "description": "API key from Resend for email notifications",
      "required": true
    }
  }
}
